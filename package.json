{
  "name": "commercecore-user-auth-service",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "npx prisma migrate deploy && node app.js",
    "build": "npx prisma generate && npx prisma migrate deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ecommerce",
    "authentication",
    "backend"
  ],
  "author": "Your Name", 
  "license": "ISC",
  "description": "Backend authentication service for an e-commerce platform",
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "prisma": "^5.19.1"
  }
}
